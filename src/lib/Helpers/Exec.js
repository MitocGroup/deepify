/**
 * Created by AlexanderC on 12/1/15.
 */

'use strict';

import {Helpers_Exec as BaseExec} from 'deep-package-manager';

export class Exec extends BaseExec {
  /**
   * @param {*} args
   */
  constructor(...args) {
    super(...args);
  }
}
